import Root from './audio-recorder.svelte';
import type { AudioRecorderProps, RecordingState } from './audio-recorder.svelte';

export {
  Root,
  type AudioRecorderProps,
  type RecordingState,
  //
  Root as AudioRecorder,
  type AudioRecorderProps as Props
};
