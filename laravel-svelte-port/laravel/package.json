{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "test": "echo 'No frontend tests'"
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}
