# Chatwoot API Base URL
# Set this to your backend API URL
# Laravel backend typically runs on port 8000
# Original Rails backend runs on port 3000 (for compatibility)
VITE_API_BASE_URL=http://127.0.0.1:8000

# WebSocket URL for Laravel Reverb
# For development: ws://localhost:8080/app/{app_key}
# For production: wss://your-domain.com/app/{app_key} (with TLS)
# Note: This is the Pusher-compatible URL format for Laravel Reverb
VITE_WS_URL=ws://127.0.0.1:8080/app/clearline-app-key

# Optional: Enable debug mode in development
# VITE_DEBUG=true
