tags:
  - Macros
operationId: list-all-macros
summary: List all macros
security:
  - userApiKey: []
description: List all macros in the account
responses:
  '200':
    description: Success
    content:
      application/json:
        schema:
          type: array
          description: 'Array of macro objects'
          items:
            $ref: '#/components/schemas/macro'
  '401':
    description: Unauthorized
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/bad_request_error'
